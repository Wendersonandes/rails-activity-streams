<%= turbo_stream.replace dom_id(@comment, :text) do %>
  <%= turbo_frame_tag dom_id(@comment, :text) do %>
    <%= render "comments/form", parent_activity: @comment.activity, comment: @comment %>
  <% end %>
<% end %>
